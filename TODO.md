# 待办

## sync 优化

### 1. 缺失 vendor 时不能报告成功

- [ ] 当前 vendor 目录或 `skills/` 目录不存在时，只打印 warning，然后继续处理其他项目。
- [ ] 所有项目都被跳过时，仍可能输出 `All skills are already latest — nothing to sync`，并以成功状态结束。
- [ ] 这会让用户误以为同步已经完成，实际没有任何 skill 被同步。
- [ ] 记录缺失或跳过的 vendor，最终返回失败状态，或者输出明确的非成功结果。

### 2. Git 查询失败不能静默当成“没有更新”

- [ ] `planVendorSync` 中的 Git 查询使用安全执行函数，失败时返回 `null`。
- [ ] 当前 `null` 可能被当成没有上游变化，导致真实的 Git 错误被隐藏。
- [ ] Git 查询失败时应明确报告错误，并让当前 vendor 的同步失败或进入明确的异常状态。
- [ ] 不能把“查询失败”和“查询成功但没有变化”混为一谈。

### 3. 获取不到 commit SHA 时不能写入无效的 `SYNC.md`

- [ ] 当前获取 skill commit SHA 失败时，仍可能写入 `Git SHA: null`。
- [ ] 后续同步无法解析这个 SHA，会反复把该 skill 判断为未同步。
- [ ] SHA 获取失败时应停止当前 skill 的同步，不写入新的 `SYNC.md`。
- [ ] 只有确认 SHA 有效后，才替换输出目录并写入同步记录。

### 4. 复制新 skill 失败时保留旧版本

- [ ] 当前流程会先删除旧的 `skills/{name}`，再复制新的 skill 文件。
- [ ] 如果复制过程中失败，旧版本也会消失，导致输出目录处于不完整状态。
- [ ] 改为先复制到临时目录，确认复制成功后再替换旧目录。
- [ ] 替换失败时尽量保留旧版本，避免同步过程破坏现有可用内容。

### 5. 只获取需要同步的 vendor

- [ ] 当前同步前会 fetch 所有 submodule，包括只用于生成文档的 source 项目。
- [ ] source 项目不参与 vendor skill 同步，属于无关网络请求。
- [ ] 无关 submodule 的网络失败也可能导致整个 sync 失败。
- [ ] 只 fetch `vendors` 中配置的 submodule，减少同步时间和失败范围。

### 6. vendor 中缺失已配置 skill 时给出明确提示

- [ ] `meta.ts` 中声明了 skill，但 vendor 仓库中不存在对应 source 目录时，当前逻辑会直接跳过。
- [ ] 旧的输出 skill 可能继续保留，但用户看不到源目录已经消失。
- [ ] 应明确提示缺失的 source skill，并说明输出目录不会被自动删除。
- [ ] 是否删除旧输出需要单独决定，不能静默处理。

### 7. 远程默认分支选择需要更可靠

- [ ] 当前找不到 `origin/HEAD` 时，会尝试 `main`、`master`，最后选择任意一个远程分支。
- [ ] 对包含多个远程分支的仓库，最后的 fallback 不一定是默认分支。
- [ ] 优先使用明确的远程默认分支信息；无法确认时应提示并跳过该 vendor，而不是猜测分支。

### 暂不处理的设计选择

- sync 不自动执行其他命令，保持自身只负责 fetch、检测变化和同步 vendor skills。
- 多个 vendor 同步时中途失败，暂不做完整回滚；需要先保留已完成项目并明确报告失败项目。
